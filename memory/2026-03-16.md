## 2026-03-16

- Reviewed funfo AI Store as a vibe-coding product.
- Main product gaps identified:
  - IA confusion between `store` (prompt/template generation gallery) and `market` (real published app store).
  - Publish/release mental model is too infrastructure-heavy for normal creators: draft/candidate/live/failed/rollback plus separate review gate.
  - Localization is inconsistent; many hardcoded strings mix Japanese, Chinese, and English outside the small i18n dictionary.
  - "Planning/questionnaire" UX exists in UI state but is not actually wired into the main generation flow.
  - Template/store credibility is weak because featured apps, usage, rating, and publisher metadata are hardcoded.
  - Post-publish owner surface exposes infrastructure/database/analytics directly, which is powerful but advanced for default creator UX.
- Build check passed with a large single JS bundle warning (~925 kB), suggesting future UX/perf pressure on the monolithic frontend.
- Added `docs/vibe-coding-product-ia-v1.md` as the new target product IA baseline.
- Implemented first-pass copy/IA soft refactor in frontend:
  - top-level naming shifted toward `Create / Build / Projects`
  - publish copy made less engineering-heavy
  - submit-review copy reframed as store application
  - infrastructure surface reframed as `Advanced`
- Replaced the hard-coded OpenClaw generation path with a provider layer built on `@mariozechner/pi-ai`.
- Added `server/llm-provider.js` and routed workspace chat streaming, repair passes, and publish backend/sql generation through it.
- Added `docs/pi-ai-provider-setup.md` with local Ollama and OpenAI configuration examples.
- Validation status:
  - `node --check server/index.js` passed
  - `node --check server/llm-provider.js` passed
  - `npm run build` passed
  - No provider is configured in this workspace yet, so generation endpoints now fail fast with setup instructions instead of falling back to OpenClaw.
- Reworked the AI provider architecture again after clarifying the requirement is platform-global configuration, not per-user keys/models.
- Added platform AI persistence in SQLite:
  - `platform_ai_providers`
  - `platform_ai_state`
  - `apps.ai_model_key`
- Implemented admin-facing platform AI controls:
  - Codex OAuth connect flow with polling + manual code fallback
  - Claude API key save/update
  - model enable/disable + provider default model
  - platform default model selection
- Implemented public model catalog endpoint so end users only see platform-exposed models.
- Wired generation flow to selected platform model:
  - store create flow
  - workspace chat/generation streaming
  - release repair
  - auto-fix
  - publish pipeline
- Validation after the platform-global refactor:
  - `node --check server/llm-provider.js` passed
  - `node --check server/index.js` passed
  - `node --check server/publish/pipeline.js` passed
  - `npm run build` passed
- Upgraded prompt/context orchestration for the vibe-coding platform:
  - `server/context-loader.js` now supports richer app/user context docs beyond plain memory, including `MISSION.md`, `SOUL.md`, `STYLE.md`, `CAPABILITIES.md`, `FAILURES.md`, and `PLAN.md`.
  - Added `server/prompt-orchestrator.js` to compose platform kernel, active agent, user context, app identity, app memory, and runtime layers per surface (`workspace`, `release`, `repair`).
  - Workspace generation, release repair, auto-fix, and publish generation now use the orchestrated prompt layer instead of relying only on the old flat base prompt.
  - Publish manifest is now persisted into app `PLAN.md`, and verifier/publish/auto-fix failures are also written into `FAILURES.md`.
  - Added new agent context docs: `planner-agent`, `workspace-agent`, and `review-agent`.
  - Validation after orchestration refactor:
    - `node --check server/context-loader.js` passed
    - `node --check server/prompt-orchestrator.js` passed
    - `node --check server/modes/workspace.js` passed
    - `node --check server/prompts.js` passed
    - `node --check server/index.js` passed
    - `node --check server/publish/pipeline.js` passed
    - `npm run build` passed
- Fixed workspace auto-fix / repair flow reliability:
  - frontend auto-fix button now respects real repair permissions, including guest-owned draft apps
  - auto-fix no longer routes every `APIError` / `NetworkError` into a fake infra restart for local/frontend-only apps
  - infra recovery branch now uses the same preview proxy path instead of injecting a direct backend URL
  - auto-fix falls back to `runRepairPass()` when the model fails to return a JSX block
  - auto-fix persistence order is now validate-first, then insert the new version, preventing broken half-fixed versions from being committed
  - validation passed after the repair-flow fix:
    - `node --check server/index.js`
    - `node --check server/modes/workspace.js`
    - `npm run build`
- Fixed a second workspace repair visibility bug for failed local apps:
  - `/api/apps/:id` now wakes local workspace runtime before responding, so failed apps can still return a real `preview_port`
  - workspace preview URL selection now prefers `preview_port` for local/failed apps instead of falling back to the public `/app/:slug` route that is blocked for failed releases
  - `api.getApp()` now throws on non-2xx responses instead of silently returning error JSON
  - auto-fix success now leaves an explicit success signal:
    - backend chat message: `AI 修复完成，已生成 vN 并刷新预览`
    - frontend success modal after preview refresh
  - verified against app `31` / workspace slug `w_q3xtz34hbld8`:
    - authenticated `GET /api/apps/31` now returns `preview_port: 10001`
    - preview at `http://127.0.0.1:10001/` returns HTTP 200 HTML
- Started the v2 platform overhaul focused on stronger coding/deployment foundations:
  - user-facing app URLs now use workspace-slug semantics:
    - preview: `/w/<workspace_slug>_preview`
    - public/live: `/w/<workspace_slug>`
    - editor moved to `/studio/<workspace_slug>`
  - backend now proxies `/w/...` routes and keeps legacy `/app/:preview_slug` compatibility
  - Vite dev server now proxies `/w` to backend so public/preview routes work from `localhost:3000`
  - preview/runtime no longer needs to expose raw `localhost:1000x` links to users
  - platform kernel prompt was generalized away from the old restaurant-specific bias
  - app context now includes structured `APP_MANIFEST.json`, written to both `server/apps/<id>/docs/` and `server/context/apps/<id>/`
  - verified on app `31`:
    - `GET /api/apps/31` returns `preview_path=/w/w_q3xtz34hbld8_preview` and `public_path=/w/w_q3xtz34hbld8`
    - `GET /w/w_q3xtz34hbld8_preview/` returns preview HTML
    - `GET /w/w_q3xtz34hbld8/` returns 409 with a clear “use preview link” message because the app is not live yet
- Implemented the first real browser-level verifier upgrade:
  - added `server/browser-smoke.js`, a headless Chrome/CDP smoke runner with no extra npm dependency
  - browser smoke now captures DOM render state, runtime/console errors, blocking network failures, simple form/button interactions, and JS dialogs
  - ignores non-actionable noise like `favicon.ico` 404s so verification is not blocked by irrelevant asset misses
  - hardened the runner with command timeouts and structured failure returns so navigation hangs do not stall the server indefinitely
- Wired browser smoke into product flows:
  - `/api/apps/:id/qa-check` now returns `browserSmoke` evidence and passes for app `31`
  - workspace `auto-fix` now browser-verifies the repaired preview before persisting a new version
  - if browser verification fails, auto-fix restores the previous runtime/preview and returns a structured 422 instead of silently keeping a broken repair
  - auto-fix now performs one extra retry using browser-verification findings as targeted repair context
  - frontend success modal now explicitly says the repair passed browser verification; failure also raises a danger modal
- Validation after the verifier-first upgrade:
  - `node --check server/browser-smoke.js` passed
  - `node --check server/verifier.js` passed
  - `node --check server/index.js` passed
  - `npm run build` passed
  - direct browser smoke against `http://127.0.0.1:3101/w/w_q3xtz34hbld8_preview/` passed with interaction smoke
- Known follow-up:
  - release `verifier-report` for some Docker-backed server apps still appears slow/hanging and needs one more pass on the server-runtime browser URL path selection / timeout behavior
- Fixed a frequent release blocker around backend SQL built with static JS string concatenation:
  - added `normalizeBackendSqlStrings()` in `server/index.js` to fold static SQL fragments like `"SELECT ..." + "FROM ..."` into one concrete SQL string before validation, runtime restore, auto-fix, and publish dry-run
  - patched the remaining workspace generation and auto-fix backend paths so they normalize backend SQL before `validateGeneratedArtifacts()` and before backend deploy
  - updated `server/validation/index.js` so it no longer falsely rejects static concatenation that can be resolved safely; it still blocks truly dynamic SQL assembly
  - tightened prompt guidance to explicitly forbid concatenation inside `db.prepare(...)`
  - verified with the previously failing sample in `server/apps/30/versions/v1/server.js` plus its schema:
    - normalization changed: true
    - rewrite count: 27
    - backend schema dry-run: ok=true, tested=22, errors=[]
  - restarted backend on `3101` after the fix
- Fixed a workspace/studio flicker loop when opening apps from My Apps:
  - root cause was preview route sync treating `/` as a meaningful `previewPath`, so the parent page kept calling `navigate()` to the same `/studio/<slug>` URL whenever the iframe posted route-change events
  - added `normalizePreviewSubPath()` in `src/pages/VibeCoding.tsx` so root preview state is normalized to `null`
  - route-sync handler now compares normalized query state and skips same-URL navigation
  - `openApp()` also now skips redundant navigation when already on the correct studio path
  - `npm run build` passed after the fix
- Continued debugging the same studio flicker and found a second, more important cause:
  - the page was actually full-reloading because opening `/studio/w_p9pzpnquzakw` triggered backend writes to `server/context/apps/33/APP_MANIFEST.json` and `server/apps/33/docs/APP_MANIFEST.json`
  - Vite was watching `server/context/**`, so those backend context writes caused dev-server full reloads
  - fixes:
    - `vite.config.ts` now ignores `server/context/**` and `server/logs/**`
    - `server/context-loader.js` `writeFile()` now skips writes when content is unchanged
    - `server/docs/index.js` `writeAppManifest()` now writes only on actual content changes and uses stable `updatedAt` instead of `new Date()` on every read
    - removed `syncAppStructuredManifest()` from the `GET /api/apps/:id` read path so simply opening a studio app no longer writes manifest files
  - restarted backend on `3101` and Vite dev server on `3000`
  - regression verification:
    - concurrent file-change watch while opening `http://127.0.0.1:3000/studio/w_p9pzpnquzakw` now shows only `server/funfo.db-wal` changing; no manifest rewrites
    - precise browser bootstrap check for `/studio/w_p9pzpnquzakw` now reports `bootCount: 1`, confirming the page itself is no longer reloading repeatedly
- Added a standalone capability/architecture HTML map for the user:
  - new file: `public/vibe-coding-platform-map.html`
  - available at `http://127.0.0.1:3000/vibe-coding-platform-map.html`
  - content includes a current capability assessment plus two diagrams:
    - app long-term lifecycle logic
    - AI generation / orchestration / validation / repair logic
- Hardened publish candidate runtime checks to reduce false `RUNTIME_HEALTH_FAILED` results:
  - `server/index.js` now has `waitRuntimeReadyDetailed()` with per-probe timeout, repeated runtime re-inspection, and structured readiness evidence
  - `server/publish/pipeline.js` candidate runtime phase now:
    - waits longer with timeout-controlled health probes
    - collects container log tails on failure
    - automatically redeploys the same artifacts once before giving up
    - probes candidate frontend through the current platform base URL instead of a hard-coded `3100`
  - `server/app-backend-manager.js` now exposes `getAppBackendLogs()`
  - `server/verifier.js` now reuses refreshed runtime info when validating runtime health
  - all modified backend files passed `node --check`
  - backend was restarted on `3101`
  - attempted a real publish regression on app 34, but the run stalled in `candidate_prepare` waiting on provider output and never reached `candidate_runtime`; the publish was then cancelled to avoid leaving the app stuck in a user-facing state
- Fixed a studio back-navigation regression:
  - the editor back button in `src/pages/VibeCoding.tsx` was using `window.location.assign('/workspace')`
  - this caused a full-page request to hit the backend origin in some cases, leading to `Cannot GET /workspace`
  - switched it to React Router `navigate(target)` so returning from `/studio/:workspaceSlug` stays inside the SPA
  - `npm run build` passed after the change
- Root-caused the persistent publish `RUNTIME_HEALTH_FAILED` issue:
  - host macOS process could not reach Docker app containers by their `172.18.x.x:3001` container IPs
  - direct host `curl http://172.18.0.3:3001/health` timed out
  - `docker exec funfo-app-34 node -e "fetch('http://127.0.0.1:3001/health')..."` returned a valid health payload, proving the app was up and the old probe path was the problem
  - fixed by changing per-app Docker containers to publish an ephemeral loopback host port (`-p 127.0.0.1::3001`) and updating platform runtime access to prefer `127.0.0.1:<hostPort>` over container IP
  - touched files:
    - `server/app-backend-manager.js`
    - `server/index.js`
    - `server/publish/pipeline.js`
    - `server/verifier.js`
  - syntax checks passed for all modified backend files
  - restarted backend on `3101`
  - verified on restored app 3 that Docker now exposes a host port (example `52177`) and `http://127.0.0.1:<hostPort>/health` returns `ok: true`
